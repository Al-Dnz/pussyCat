 <% @order.items.each do |item| %>
<%= image_tag item.cat, :alt => "cat-pic" %>   <br>
   <%= item.title %>
   <br>
   <%= item.description %>
   <br>
   <%=  item.price %> €
   <br>

   <br>

   <br> <br>
 <% end %>
